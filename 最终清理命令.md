# 最终清理命令 - 保存此文件

**每天自动运行两次：中午12:00 和 凌晨02:00**

---

## 🎯 执行命令（保存这两个）

### 命令1：清理 Counterparty + Group

```bash
cd "/Users/mac/api auto/api_auto_framework" && python3 cleanup_counterparty.py
```

### 命令2：清理 Contact + Sub Account + FBO

```bash
cd "/Users/mac/api auto/api_auto_framework" && python3 cleanup_contact_sub_fbo.py
```

---

## 📅 备份表命名规则（已更新）

### 格式：`{表名}_{日期}_{时间}`

**示例**：
- `backup_data.actc_t_share_recipient_0210_1200` → 2月10日 12:00
- `backup_data.actc_t_share_recipient_0210_0200` → 2月10日 02:00
- `backup_data.actc_contact_0210_1200` → 2月10日 12:00
- `backup_data.actc_contact_0210_0200` → 2月10日 02:00

### 好处
- ✅ **不会覆盖** - 中午12点和凌晨2点的备份都保留
- ✅ **清晰可辨** - 一眼看出备份时间
- ✅ **便于恢复** - 可以选择特定时间点的备份

---

## 🔄 执行流程

```
1. 全量备份（整个表） → backup_data.actc_*_0210_1200
   ↓ 失败则终止
2. 扫描测试数据（"Auto TestYan"开头）
   ↓
3. 显示统计
   ↓
4. 等待确认（输入yes）
   ↓
5. 执行删除（只删除测试数据）
   ↓
6. 完成
```

---

## 💾 备份数据示例

### 每天的备份表

```
backup_data.actc_t_share_recipient_0209_1200   (2月9日 中午)
backup_data.actc_t_share_recipient_0209_0200   (2月9日 凌晨)
backup_data.actc_t_share_recipient_0210_1200   (2月10日 中午)
backup_data.actc_t_share_recipient_0210_0200   (2月10日 凌晨)
...
```

### 查看备份表
```sql
SELECT table_name FROM information_schema.tables 
WHERE table_schema = 'backup_data' 
  AND table_name LIKE 'actc_%'
ORDER BY table_name DESC;
```

---

## 🔄 恢复数据（如需要）

```sql
-- 恢复中午12点的备份
INSERT INTO actc.t_share_recipient
SELECT * FROM backup_data.actc_t_share_recipient_0210_1200;

-- 或恢复凌晨2点的备份
INSERT INTO actc.t_share_recipient
SELECT * FROM backup_data.actc_t_share_recipient_0210_0200;
```

---

## ⚠️ 重要提醒

1. **先备份后删除** - 备份失败则终止清理
2. **全量备份** - 备份整个表（包括所有数据）
3. **只删测试数据** - 只删除 "Auto TestYan" 开头的数据
4. **不会覆盖** - 不同时间的备份都会保留
5. **安全可靠** - 使用开发提供的备份函数

---

**保存此文件，每天自动运行时使用！** 📌
