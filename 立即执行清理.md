# 立即执行清理 - 快速命令

**保存这两个命令，定期执行即可** 📌

---

## 1️⃣ 清理 Counterparty + Group

```bash
cd "/Users/mac/api auto/api_auto_framework" && python3 cleanup_counterparty.py
```

**清理**: Counterparty（收款人）+ Counterparty Group（收款人组）  
**备份**: ✅ 自动备份到 `backup_data.actc_*_{日期}`

---

## 2️⃣ 清理 Contact + Sub Account + FBO

```bash
cd "/Users/mac/api auto/api_auto_framework" && python3 cleanup_contact_sub_fbo.py
```

**清理**: Contact（联系人）+ Sub Account（子账户）+ FBO Account（FBO账户）  
**备份**: ✅ 自动备份到 `backup_data.actc_*_{日期}`

---

## ✅ 自动清理功能已集成

### 在测试中使用

```python
def test_create_counterparty(counterparty_api, db_cleanup):
    """创建Counterparty - 测试结束后自动清理"""
    
    # 1. 创建资源
    response = counterparty_api.create_counterparty(
        recipient_name="Auto TestYan Counterparty 001",  # ⚠️ 必须此前缀
        email_address="auto.testyan@example.com",
        ...
    )
    
    # 2. 提取ID
    created_id = response.json()["data"]["id"]
    
    # 3. ⚠️ 关键：跟踪ID（测试结束后自动清理）
    from test_cases.conftest import track_created_id
    track_created_id("counterparty", created_id)
    
    # 4. 继续测试...
    # 无需手动删除，测试会话结束后自动清理！
```

### 支持的模块

自动清理支持以下模块：
- `counterparty`
- `counterparty_group`
- `contact`
- `sub_account`
- `fbo_account`
- `financial_account`
- `trading_order`
- `client_list`
- `card`
- `user`

---

## 📊 功能验证

✅ **自动清理已集成** - `test_cases/conftest.py` 中的 `db_cleanup` fixture  
✅ **track_created_id 可用** - 验证通过  
✅ **模块字典已配置** - 包含所有需要的模块  
✅ **清理脚本语法正确** - 验证通过  

---

## 💡 使用建议

### 日常清理（每周一次）
```bash
# 周一执行
cd "/Users/mac/api auto/api_auto_framework"
python3 cleanup_counterparty.py
python3 cleanup_contact_sub_fbo.py
```

### 自动清理（推荐）
在所有创建资源的测试中添加 `db_cleanup` 参数和 `track_created_id()` 调用。

### 备份保留
建议保留备份表24小时后再删除。

---

**就这么简单！** 🎉
