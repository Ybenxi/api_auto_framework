# 测试数据命名规范修复报告

**修复日期**: 2026-02-09  
**修复原因**: 确保所有测试数据都以 "Auto TestYan" 开头，方便自动清理

---

## ✅ 修复完成（7个文件）

### 1. Contact 模块

**文件**: `test_cases/contact/test_contact_update_contact_detail.py`

**修复内容**:
- ❌ `"MultiUpdate"` → ✅ `"Auto TestYan MultiUpdate {timestamp}"`
- ❌ `"Test"` → ✅ `"Auto TestYan Test"`
- ❌ `f"Updated_{original_first_name}"` → ✅ `f"Auto TestYan Updated {timestamp}"`
- ❌ `"Invalid"` → ✅ `"Auto TestYan InvalidTest"`

**影响测试**: 4个测试场景

---

### 2. User Sign Up - Create User

**文件**: `test_cases/user_signup/test_user_signup_create_user.py`

**修复内容**:
- ❌ `first_name="Existing"` → ✅ `first_name="Auto TestYan Existing"`
- ❌ `last_name="User"` → ✅ `last_name="Auto TestYan User"`
- ❌ `first_name="Test"` → ✅ `first_name="Auto TestYan Test"`（多处）

**影响测试**: 6个测试场景

---

### 3. User Sign Up - Complete Flow

**文件**: `test_cases/user_signup/test_user_signup_complete_flow.py`

**修复内容**:
- ❌ `first_name="Existing"` → ✅ `first_name="Auto TestYan Existing"`
- ❌ `last_name="User"` → ✅ `last_name="Auto TestYan User"`
- ❌ `first_name="Test"` → ✅ `first_name="Auto TestYan Test"`（多处）

**影响测试**: 3个测试场景

---

### 4. Card Management - Operations

**文件**: `test_cases/card/card_management/test_card_operations.py`

**修复内容**:
- ❌ `first_name="Updated"` → ✅ `first_name="Auto TestYan Updated {timestamp}"`
- ❌ `last_name="Name"` → ✅ `last_name="Auto TestYan Name"`

**影响测试**: 2个测试场景

---

### 5. Card Opening - Reward Card

**文件**: `test_cases/card/card_opening/test_card_opening_reward_card.py`

**修复内容**:
- ❌ `first_name="Test"` → ✅ `first_name="Auto TestYan Test"`（多处）
- ❌ `last_name="User"` → ✅ `last_name="Auto TestYan User"`（多处）

**影响测试**: 3个测试场景

---

### 6. Wire Processing - Counterparties

**文件**: `test_cases/payment_deposit/wire_processing/test_wire_counterparties.py`

**修复内容**:
- ❌ `bank_account_owner_name="Test Company"` → ✅ `"Auto TestYan Company"`
- ❌ `bank_name="Test Bank"` → ✅ `"Auto TestYan Bank"`
- ❌ `name="Test"` → ✅ `name="Auto TestYan Test"`（多处）
- ❌ `bank_account_owner_name="Test"` → ✅ `"Auto TestYan Test"`

**影响测试**: 3个测试场景

---

### 7. Remote Deposit Check - Counterparties

**文件**: `test_cases/payment_deposit/remote_deposit_check/test_check_counterparties.py`

**修复内容**:
- ❌ `name="Test"` → ✅ `name="Auto TestYan Test"`

**影响测试**: 1个测试场景

---

## 📊 修复统计

- **修复文件数**: 7个
- **影响测试场景**: 22个
- **修复的硬编码值**: 20+个

---

## ⚠️ 仍需手动清理的脏数据

由于这些测试已经运行过，数据库中可能已经存在以下脏数据：

### Contact 表
- first_name = "MultiUpdate"
- first_name = "Updated_XXX" (各种叠加)
- first_name = "Invalid"
- first_name = "Auto TestYan InvalidPhone"

### User 表
- first_name = "Test"
- first_name = "Existing"
- last_name = "User"

### Card 表
- first_name = "Updated"
- first_name = "Test"
- last_name = "Name", "User"

### Counterparty 表（Payment模块）
- name = "Test"
- bank_account_owner_name = "Test Company", "Test"

---

## 🗑️ 清理建议

### 立即清理 Counterparty
```bash
cd "/Users/mac/api auto/api_auto_framework"
python3 cleanup_counterparty.py
```

### 手动清理其他模块
由于其他表名未配置，需要：
1. 查询数据库确认表名
2. 更新清理规则
3. 执行清理

---

## ✅ 后续保障

修复后：
- ✅ 所有新创建的数据都以 "Auto TestYan" 开头
- ✅ 可以使用自动清理工具批量清理
- ✅ 不会再产生难以识别的脏数据

---

## 📝 注意事项

1. **这些测试大部分已标记为 skip**，不会实际执行，所以产生脏数据的可能性较低
2. **Update类测试**会更新已有数据，现在更新后的值也符合规范了
3. **错误处理测试**使用的假数据通常不会真正创建成功

---

**修复完成！** 所有测试数据现在都符合 "Auto TestYan" 命名规范。
